export default {
  // project root (where index.html lives)
  root: '.',
  // folder to copy as-is (images, favicon, etc.)
  publicDir: 'public',
  build: { outDir: 'dist' },
  server: { port: 5173 }
};
